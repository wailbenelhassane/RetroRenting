import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-app-base',
  webDir: 'www',
 /* server: {
    hostname: '127.0.0.1',
    cleartext: true,
    allowNavigation: ['*']
  }
  Descomentar por si ayuda en la ejecución del emulador */
};

export default config;

